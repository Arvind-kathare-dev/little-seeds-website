import EnrichmentPageDesign from "@/components/EnrichmentPageDesign";

export const metadata = {
  title: "Enrichment Programs — Little Seeds",
  description: "Year-round growth & fun. Summer Program, Off-Track Program, Before & After School.",
};

export default function EnrichmentPage() {
  return <EnrichmentPageDesign />;
}
