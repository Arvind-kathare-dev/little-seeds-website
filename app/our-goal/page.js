import OurGoal from "@/components/OurGoal";

export const metadata = {
  title: "Our Goal — Little Seeds",
  description: "Our goal at Little Seeds is to provide faith-centered, inclusive learning rooted in Islamic values and modern education.",
};

export default function OurGoalPage() {
  return <OurGoal />;
}
